<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.asura.ddd.structure.inventory.infrastructure.persistence.mapper.InventoryMapper">

    <select id="selectPageByCondition" resultType="org.asura.ddd.structure.inventory.infrastructure.persistence.entity.InventoryEntity">
        SELECT id, product_id, quantity, reserved_quantity, tenant_id, 
               version, deleted, created_by, created_at, updated_by, updated_at
        FROM t_inventory
        WHERE deleted = FALSE
        <if test="productId != null and productId != ''">
            AND product_id LIKE '%' || #{productId} || '%'
        </if>
        ORDER BY created_at DESC
    </select>

    <select id="selectByProductId" resultType="org.asura.ddd.structure.inventory.infrastructure.persistence.entity.InventoryEntity">
        SELECT id, product_id, quantity, reserved_quantity, tenant_id, 
               version, deleted, created_by, created_at, updated_by, updated_at
        FROM t_inventory 
        WHERE product_id = #{productId} AND deleted = FALSE
    </select>

</mapper>